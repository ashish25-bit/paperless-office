1. On clicking the connect button the data is being entered into the database but the browser is needed to be refreshed to allow the user to disconnect. If not refreshing same values is being entered into the database

2. The remove background api not working properly. It is working after you login again.

3. When the user is logged in the connect button is showing that the 'Connect' option even if it is already 'Conneceted'. The page is needed to be refreshed to get the correct state. You have to use Joining of ths database.


// chat app
1. As soon as the user opens the chat a chat room will be formed. 
   For eg: user 1 => id = 3
           user 2 => id = 1
           chat room id = 1+3(string)
2. User 3 creates the room and if the user 1 also open chat with user, then he will be added to the room 1+3
3. If the user 1 does not join the room the message sent by user 3 will be stored in the database and user 1 will be notified about the msg.

// for chats -> functions required for socket.io
1. make the room
2. check whether the room is present and if present join the room
3. As soon as both the users leaves the chat room should be canceled 